import styled, { css } from 'styled-components';

const PorfolioChi = () => css`
  font-family: Helvetica, sans-serif;
	color: #000;
`;


export const PorfolioChiContainer = styled.div`
	${PorfolioChi}
`;